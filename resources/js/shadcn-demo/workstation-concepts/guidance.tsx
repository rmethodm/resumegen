import React, { useState } from 'react';
import { createRoot } from 'react-dom/client';
import '../../../css/app.css';
import { ArrowRight, BriefcaseBusiness, Check, ChevronRight, FileText, LayoutDashboard, ListChecks, Sparkles, StickyNote } from 'lucide-react';
import { Button } from '@/Components/ui/button';
import { Badge } from '@/Components/ui/badge';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@/Components/ui/card';
import { Tabs, TabsList, TabsTrigger } from '@/Components/ui/tabs';
import { Textarea } from '@/Components/ui/textarea';
import { Input } from '@/Components/ui/input';
import { Label } from '@/Components/ui/label';
import { Select } from '@/Components/ui/select';
import { Separator } from '@/Components/ui/separator';
import { Alert, AlertTitle, AlertDescription } from '@/Components/ui/alert';
import { SidebarProvider, Sidebar, SidebarHeader, SidebarContent, SidebarGroup, SidebarGroupLabel, SidebarMenu, SidebarMenuItem, SidebarMenuButton, SidebarFooter, SidebarTrigger } from '@/Components/ui/sidebar';
import { Sheet, SheetContent, SheetHeader, SheetTitle } from '@/Components/ui/sheet';
import { cn } from '@/lib/utils';

const directions = [
    ['Contextual companion', 'Keep the full form visible. Guidance follows the section you are editing.'],
    ['Requirement workbench', 'Start with a job requirement, then strengthen the experience that supports it.'],
    ['Focused editor', 'Work on one section at a time with a quiet, persistent job brief.'],
];
const sections = ['Summary', 'Experience', 'Skills'] as const;
type Section = typeof sections[number];
const initial: Record<Section, string> = {
    Summary: 'Product designer with experience simplifying complex workflows, collaborating with engineering, and building accessible products.',
    Experience: 'Led the redesign of an onboarding workflow, partnering with product and engineering. Reduced setup time by 28%.\n\nCreated reusable components and documented interaction patterns for the product team.',
    Skills: 'Product design · Figma · Prototyping · Design systems · Accessibility · User research',
};
const prompts: Record<Section, string> = {
    Summary: 'The role emphasizes complex workflows. Lead with the kind of product problems you have solved.',
    Experience: 'Your onboarding result is relevant. Explain your role and what changed for the user.',
    Skills: 'The job asks for accessible interaction design. Include only skills you can support with examples.',
};
const proposed = 'Simplified a complex onboarding workflow with product and engineering, reducing setup time by 28%. Documented reusable components and interaction patterns for the product team.';
function App() {
    const [direction, setDirection] = useState('0');
    const [stage, setStage] = useState('edit');
    const [section, setSection] = useState<Section>('Experience');
    const [tailoredValues, setTailoredValues] = useState(initial);
    const [baseValues, setBaseValues] = useState(initial);
    const [notes, setNotes] = useState('Ask how the design team measures onboarding success.');
    const [ai, setAi] = useState(false);
    const [decision, setDecision] = useState('');
    const [base, setBase] = useState(false);
    const values = base ? baseValues : tailoredValues;
    const setValues = base ? setBaseValues : setTailoredValues;
    const [drawer, setDrawer] = useState(false);
    const [requirement, setRequirement] = useState(0);
    const requirements = ['Simplify complex workflows', 'Build accessible design systems', 'Collaborate across functions'];
    const edit = (key: Section, text: string) => setValues(v => ({ ...v, [key]: text }));
    const fields = (single = false) => <div className="flex flex-col gap-4">{(single ? [section] : sections).map(key => <Card key={key} className={cn(section === key && 'ring-1 ring-ring/30')}><CardHeader><div className="flex items-center justify-between"><CardTitle>{key}</CardTitle><Badge variant="outline">{key === 'Experience' ? 'Northwind · Product designer' : 'Resume section'}</Badge></div></CardHeader><CardContent className="flex flex-col gap-3">{key === 'Experience' && <><Label htmlFor={`role-${direction}`}>Role</Label><Input id={`role-${direction}`} defaultValue="Product designer · Northwind" onFocus={() => setSection(key)} /></>}<Label htmlFor={`field-${key}`}>{key === 'Experience' ? 'Achievements' : key === 'Summary' ? 'Professional summary' : 'Your skills'}</Label><Textarea id={`field-${key}`} value={values[key]} onFocus={() => setSection(key)} onChange={e => edit(key, e.target.value)} rows={single ? 9 : key === 'Experience' ? 6 : 3}/><p className="text-xs text-muted-foreground">{key === 'Experience' ? 'Describe what you did, how you did it, and the outcome.' : 'Keep it specific and true to your experience.'}</p></CardContent></Card>)}</div>;
    const guidance = <div className="flex flex-col gap-5"><div><div className="flex items-center gap-2 text-sm font-medium"><BriefcaseBusiness className="size-4" />{base ? 'Your base resume' : 'Target job'}</div><h3 className="mt-3 text-lg font-semibold">{base ? 'Ready for your next opportunity' : 'Senior Product Designer'}</h3><p className="text-sm text-muted-foreground">{base ? 'Build a strong foundation before tailoring.' : 'Meridian · Remote · Product team'}</p></div><Separator/><div className="flex flex-col gap-3"><Badge variant="secondary" className="self-start">Editing {section.toLowerCase()}</Badge><h4 className="font-medium">{base ? 'Make this section stronger' : 'What matters for this role'}</h4><p className="text-sm leading-6 text-muted-foreground">{prompts[section]}</p><Alert><ListChecks/><AlertTitle>One useful next step</AlertTitle><AlertDescription>{section === 'Experience' ? 'You have a measurable result. Add the scope of your ownership if you can substantiate it.' : section === 'Summary' ? 'Name a relevant strength and the kind of impact you bring.' : 'Connect each specialist skill to experience elsewhere in your resume.'}</AlertDescription></Alert></div>{!base && <details className="text-sm"><summary className="cursor-pointer font-medium">Read the job description</summary><p className="mt-3 leading-6 text-muted-foreground">Meridian is looking for a senior designer to simplify complex workflows, build accessible design systems, and collaborate with product and engineering. You will own projects from discovery through delivery.</p></details>}<Separator/><div className="flex flex-col gap-3"><div className="flex items-center justify-between"><h4 className="font-medium">Writing assistance</h4><Badge variant="outline">Sample AI</Badge></div><p className="text-sm text-muted-foreground">Review a suggested experience rewrite. Your original stays unchanged until you accept.</p>{!ai ? <Button variant="outline" onClick={() => {setAi(true);setDecision('');}}><Sparkles/>Show sample suggestion</Button> : <><p className="rounded-md border bg-muted/40 p-3 text-sm leading-6">{proposed}</p><div className="flex gap-2"><Button size="sm" onClick={() => {edit('Experience', proposed);setDecision('Suggestion accepted into Experience.');setAi(false);}}>Accept</Button><Button size="sm" variant="outline" onClick={() => {setAi(false);setDecision('Suggestion discarded.');}}>Discard</Button></div></>}{decision && <p role="status" className="text-sm">{decision}</p>}</div><Separator/><Label htmlFor="job-notes"><StickyNote className="size-4"/> Application notes</Label><Textarea id="job-notes" value={notes} onChange={e => setNotes(e.target.value)} rows={3}/><p className="text-xs text-muted-foreground">Fictional example. Edits stay in this page session; no credits are used.</p></div>;
    return <SidebarProvider style={{'--sidebar-width':'13rem'} as React.CSSProperties}><Sidebar><SidebarHeader className="p-4"><span className="font-semibold">Resumegen</span><span className="text-xs text-muted-foreground">Your next chapter</span></SidebarHeader><SidebarContent><SidebarGroup><SidebarGroupLabel>Workspace</SidebarGroupLabel><SidebarMenu>{[[LayoutDashboard,'Dashboard'],[BriefcaseBusiness,'Jobs'],[FileText,'Resumes'],[ListChecks,'Applications']].map(([Icon, label]) => { const Glyph = Icon as typeof FileText;return <SidebarMenuItem key={String(label)}><SidebarMenuButton isActive={label === 'Resumes'} asChild><a href={label === 'Jobs' || label === 'Applications' ? './application-flow.html' : './index.html'}><Glyph/><span>{String(label)}</span></a></SidebarMenuButton></SidebarMenuItem>;})}</SidebarMenu></SidebarGroup></SidebarContent><SidebarFooter className="p-4"><span className="text-sm font-medium">Jordan Rivera</span><span className="text-xs text-muted-foreground">Design preview</span></SidebarFooter></Sidebar><main className="min-w-0 flex-1"><div className="flex flex-wrap items-center justify-between gap-3 border-b bg-muted/35 px-4 py-3"><div className="flex items-center gap-2"><SidebarTrigger/><Badge variant="outline">Layout exploration</Badge></div><Tabs value={direction} onValueChange={setDirection}><TabsList className="h-auto flex-wrap">{directions.map(([name], i) => <TabsTrigger key={name} value={String(i)}>{i+1}. {name}</TabsTrigger>)}</TabsList></Tabs><p className="text-xs text-muted-foreground">{directions[+direction][1]}</p></div><div className="flex flex-col gap-5 p-4"><div className="flex items-center gap-2 text-xs text-muted-foreground">Jobs<ChevronRight className="size-3"/>Meridian<ChevronRight className="size-3"/>Prepare application</div><header className="flex flex-wrap items-center justify-between gap-4"><div><h1 className="text-2xl font-semibold tracking-tight">{base ? 'Build your base resume' : 'Make your experience relevant.'}</h1><p className="mt-1 text-sm text-muted-foreground">{base ? 'A reusable foundation for future applications.' : 'Senior Product Designer at Meridian'}</p></div><Button onClick={() => setStage(stage === 'review' ? 'edit' : 'review')}>{stage === 'review' ? 'Back to editing' : 'Review & apply'}<ArrowRight/></Button></header><div className="flex flex-wrap items-center justify-between gap-3 border-y py-3"><Select aria-label="Resume version" value={base ? 'base' : 'job'} onChange={e => setBase(e.target.value === 'base')}><option value="job">Meridian · Tailored resume</option><option value="base">Product design · Base resume</option></Select><span className="flex items-center gap-2 text-xs text-muted-foreground"><Check className="size-3"/> Session edits retained · Independent versions</span><Button className="lg:hidden" variant="outline" onClick={() => setDrawer(true)}>Job guidance</Button></div><Tabs value={stage} onValueChange={setStage}><TabsList><TabsTrigger value="edit">Edit resume</TabsTrigger><TabsTrigger value="review">Review & apply</TabsTrigger></TabsList></Tabs>{stage === 'review' ? <div className="grid gap-6 lg:grid-cols-[minmax(0,1fr)_340px]"><Card><CardContent className="flex flex-col gap-6 p-8 sm:p-12"><div><h2 className="text-2xl font-semibold">Jordan Rivera</h2><p>Product designer</p><p className="text-sm text-muted-foreground">Austin, TX · jordan@example.com</p></div>{sections.map(s => <section key={s}><h3 className="mb-3 text-sm font-semibold uppercase tracking-wider">{s}</h3><p className="whitespace-pre-line text-sm leading-7">{values[s]}</p></section>)}</CardContent></Card><div className="flex flex-col gap-4"><h2 className="text-lg font-semibold">Your application, ready to review</h2><p className="text-sm leading-6 text-muted-foreground">The document preview lives here, after editing. Check your wording and layout before visiting the employer.</p><Alert><AlertTitle>Preview only</AlertTitle><AlertDescription>No application is submitted from this mockup.</AlertDescription></Alert><Button variant="outline" onClick={() => setStage('edit')}>Return to editor</Button></div></div> : direction === '1' ? <div className="grid items-start gap-5 lg:grid-cols-[240px_minmax(0,1fr)_320px]"><Card><CardHeader><CardTitle>Job requirements</CardTitle><CardDescription>Choose what to work on.</CardDescription></CardHeader><CardContent className="flex flex-col gap-2">{requirements.map((r,i) => <Button key={r} className="h-auto justify-start whitespace-normal py-3 text-left" variant={requirement === i ? 'secondary':'ghost'} onClick={() => {setRequirement(i);setSection(i===1?'Skills':'Experience');}}>{r}</Button>)}</CardContent></Card><div className="flex flex-col gap-4"><Alert><AlertTitle>{requirements[requirement]}</AlertTitle><AlertDescription>Connect this requirement to evidence in your own experience.</AlertDescription></Alert>{fields(true)}</div><aside className="hidden border-l pl-5 lg:block">{!drawer && guidance}</aside></div> : direction === '2' ? <div className="grid items-start gap-6 lg:grid-cols-[minmax(0,1fr)_340px]"><div className="flex flex-col gap-5"><Tabs value={section} onValueChange={v => setSection(v as Section)}><TabsList>{sections.map((s,i) => <TabsTrigger value={s} key={s}>{i+1}. {s}</TabsTrigger>)}</TabsList></Tabs>{fields(true)}<div className="flex items-center justify-between"><p className="text-sm text-muted-foreground">One section at a time. Your place is preserved.</p><Button variant="outline" onClick={() => sections.indexOf(section)<2 ? setSection(sections[sections.indexOf(section)+1]) : setStage('review')}>{section === 'Skills' ? 'Review resume' : 'Next section'}<ArrowRight/></Button></div></div><aside className="hidden border-l pl-6 lg:block">{!drawer && guidance}</aside></div> : <div className="grid items-start gap-6 lg:grid-cols-[minmax(0,1.65fr)_minmax(300px,1fr)]">{fields()}<aside className="sticky top-4 hidden max-h-[calc(100vh-2rem)] overflow-auto border-l pl-6 pr-1 lg:block">{!drawer && guidance}</aside></div>}</div></main><Sheet open={drawer} onOpenChange={setDrawer}><SheetContent className="overflow-auto"><SheetHeader><SheetTitle>Job guidance</SheetTitle><p className="text-sm text-muted-foreground">Context for the section you are editing.</p></SheetHeader><div className="p-4">{guidance}</div></SheetContent></Sheet></SidebarProvider>;
}
createRoot(document.getElementById('root')!).render(<App/>);
